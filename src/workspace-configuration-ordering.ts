/** Reorders or removes selected configurations. */
import { assertConfigurationIndex, configurationTarget } from "./workspace-configuration-target.js";
import { loadWorkspace, saveWorkspace, type ConfigurationPositionOptions, type MoveConfigurationOptions, type PolicyWorkspace } from "./workspace.js";
export function removeConfigurationFromWorkspace(path: string, options: ConfigurationPositionOptions): PolicyWorkspace { const workspace = loadWorkspace(path); const configurations = configurationTarget(workspace, options).configurations; assertConfigurationIndex(configurations, options); configurations.splice(options.configurationIndex, 1); saveWorkspace(path, workspace); return workspace; }
export function moveConfigurationInWorkspace(path: string, options: MoveConfigurationOptions): PolicyWorkspace { const workspace = loadWorkspace(path); const configurations = configurationTarget(workspace, options).configurations; assertConfigurationIndex(configurations, options); const next = options.direction === "up" ? options.configurationIndex - 1 : options.configurationIndex + 1; if (next >= 0 && next < configurations.length) { const [entry] = configurations.splice(options.configurationIndex, 1); configurations.splice(next, 0, entry); } saveWorkspace(path, workspace); return workspace; }
